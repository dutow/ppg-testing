---
dependency:
  name: galaxy
driver:
  name: ${driver}
platforms:
  - name: pgtde-rocky10-${BUILD_NUMBER}
    region: ${region}
    image: ${ami_rocky10_x86_64}
    vpc_subnet_id: ${vpc_subnet_id_aws2}
    instance_type: t3.xlarge
    ssh_user: rocky
    root_device_name: /dev/sda1
    instance_tags:
      iit-billing-tag: jenkins-pg-molecule
provisioner:
  name: ansible
  log: True
  playbooks:
    create: ../../../../playbooks/create.yml
    destroy: ../../../../playbooks/destroy.yml
    prepare: ../../../../playbooks/prepare.yml
    converge: ../../playbooks/playbook.yml
verifier:
  name: ansible
scenario:
  destroy_sequence:
    - destroy
  cleanup_sequence:
    - cleanup
  test_sequence:
    - destroy
    - create
    - prepare
    - converge
    - destroy
